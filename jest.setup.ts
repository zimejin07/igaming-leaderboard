// Import the environment setup script.
// needed before running tests. Make sure "setup-env" exists in your project.
import "./setup-env";

// Import custom Jest matchers from @testing-library/jest-dom.
// These matchers allow for more intuitive assertions when testing DOM elements,
// such as checking if an element is visible or contains text content.
import "@testing-library/jest-dom";
